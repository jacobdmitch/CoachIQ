# Frontend env — all vars must be prefixed REACT_APP_ to be injected by CRA.
# Leave REACT_APP_SENTRY_DSN unset to disable Sentry; the initSentry() call
# becomes a noop.
REACT_APP_SENTRY_DSN=
REACT_APP_RELEASE=
